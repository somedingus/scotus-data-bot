# local Claude Code config
.claude/
.vscode/

# python
__pycache__/
*.pyc

# data: raw API dumps + processed staging are local-only / regenerable
data/

# database artifacts (regenerable; distributed via GitHub Release)
*.sqlite
*.sqlite-journal
*.sqlite.gz
SHA256SUMS

# secrets — never commit the CourtListener token
*.token
.env

# pytest
.pytest_cache/
